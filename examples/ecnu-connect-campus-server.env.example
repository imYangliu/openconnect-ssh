# Copy this file to ~/.config/ecnu-connect-campus-server.env
# or /etc/ecnu-ssh/connect-campus-server.env and fill in your own values.

VPN_HOST=vpn.example.com
VPN_USER=your_vpn_username

# Optional for installations that require an auth group / realm selection.
# VPN_AUTHGROUP=your_auth_group

# Replace these routes with the private subnets you want to reach through VPN.
VPN_ROUTES="x.x.x.x/yy a.b.c.d/zz"

# Optional target used by `connect-campus-server.sh verify` and by `ecnu-ssh`.
TARGET_HOST=your-campus-host.example
TARGET_PORT=22
TARGET_SSH_USER=your-ssh-user

# Optional: put your password here only if you accept the security trade-off.
# VPN_PASSWORD=

# Optional overrides.
# OPENCONNECT_BIN=/usr/sbin/openconnect
# VPN_SCRIPT_CMD=/usr/local/bin/vpn-slice x.x.x.x/yy a.b.c.d/zz
# PID_FILE=/run/ecnu-openconnect.pid
# LOG_FILE=/var/log/ecnu-openconnect.log
